# NeoPDF's interface to Wolfram Alpha

This crate provides a Rust interface to the Wolfram Language.

To build, simply run the following command:
```bash
cargo build --release --manifest-path neopdf_wolfram/Cargo.toml
```
